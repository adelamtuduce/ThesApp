<div class="col-md-12">
	<div class="col-md-6">
		<div class="panel panel-default acceptedEnrollRequests" data-teacher-id="<%= @teacher.id %>">
		  <!-- Default panel contents -->
		  	<div class="panel-heading clearfix">Your Accepted Enrollments
			</div>
		  	<div class="panel-body informationPanel">
				<table class="table acceptedEnrollRequestsTable" class="display" data-source="<%=accepted_requests_teacher_path(@teacher.id)%>">
					<thead>
						<th style="text-align:left">Student</th>
						<th>Project Name</th>
						<th>Actions</th>
					</thead>
					<tbody>
						
					</tbody>
				</table>
			</div>
		</div>
	</div>

	<div class="col-md-6">
		<div class="panel panel-default enrollRequests" data-teacher-id="<%= @teacher.id %>">
		  <!-- Default panel contents -->
		  	<div class="panel-heading clearfix">Your Enrollment Requests
			</div>
		  	<div class="panel-body informationPanel">
				<table class="table enrollRequestsTable" class="display" data-source="<%= enrolls_teacher_path(@teacher.id) %>">
					<thead>
						<th style="text-align:left">Student</th>
						<th>Project Name</th>
						<th>Actions</th>
					</thead>
					<tbody>
						
					</tbody>
				</table>
			</div>
		</div>
	</div>
</div>
